package com.succedant.rest.json;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativecustomerResourcesIT extends customerResourcesTest {

    // Execute the same tests but in native mode.
}